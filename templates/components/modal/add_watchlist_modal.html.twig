<div class="modal fade" id="addWatchlistModal" tabindex="-1" aria-labelledby="addWatchlistModalLabel"
     aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="addWatchlistModalLabel">
                    {{ 'forms.watchlist.add.title'|trans }}
                </h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <turbo-frame id="add_watchlist_form" src="{{ path('app_movie_watchlists_add') }}"
                             loading="lazy"></turbo-frame>
            </div>
        </div>
    </div>
</div>
